#include <strace.h>

////////////////////////////////////////////////////////////////////////////////
/// PUBLIC FUNCTION
////////////////////////////////////////////////////////////////////////////////

int strace(const char *filename)
{
	int result;
	void (*handler)(void);

	if ((result = get_handler(&handler, filename)) != SUCCESS)
		return error(result);

	handler();

	return SUCCESS;
}
